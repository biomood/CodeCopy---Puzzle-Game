require "dingoo_font"

-- returns menu table with list of choices
local function createMenu(choices)
end

-- displays the menu
local function drawMenu(menu)
end

-- moves the mene item
-- returns value of selected item if any
local function keyReleased(menu)
end


menu = {
	createMenu = createMenu,
	drawMenu = drawMenu,
	keyReleased = keyReleased
}